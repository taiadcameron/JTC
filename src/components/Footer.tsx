"use client";

const Footer = () => {
  return (
    <footer className="bg-gray-800 p-4 text-white text-center">
      <p>&copy; {new Date().getFullYear()} Join the Club</p>
    </footer>
  );
};

export default Footer;
